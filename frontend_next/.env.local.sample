# Exemplo de arquivo de variáveis de ambiente para desenvolvimento
# NÃO commite este arquivo com chaves reais. Copie para `.env.local` e preencha.

# Configuração de ambiente mínima para desenvolvimento local
# Para execução local com backend Python/SQLite, configure:
# Exemplo: DATABASE_URL=sqlite:///./db/tradecomigo.sqlite3
# Se você usa um backend remoto, configure ENDPOINTs e chaves do serviço externo.
# NEXT_PUBLIC_API_URL=http://localhost:3000

# Chave do LLM (ex: GROQ)
LLM_API_KEY=sk-xxxx
GROQ_MODEL=llama-3.1-8b-instant

# Outros ajustes (opcionais)
PAGE_TIMEOUT=30000
PAGE_LOAD_WAIT=3000
ENABLE_CACHE=true
CACHE_DIR=./cache
OUTPUT_DIR=./output
LOG_DIR=./logs

# Instruções rápidas:
# 1) Copie este arquivo para `frontend_next/.env.local`
# 2) Preencha `SUPABASE_URL` e `SUPABASE_ANON_KEY` com os valores do painel Supabase
# 3) Reinicie o servidor de desenvolvimento Next.js: `cd frontend_next && npm run dev`