# ---------------------------
# Example environment variables for TradeComigo
# ---------------------------
# Notes:
# - Use `.env.example` as the reference for required variables.
# - Store secrets in your deployment's secret store (Azure Key Vault, AWS Secrets Manager,
#   GitHub Actions secrets, etc.) and inject them at deploy time.

# ---------------------------
# RAG / Vector DB (Search by embedding)
# ---------------------------
# Provider choices: chromadb | none
# - chromadb: local lightweight vector DB (good for dev)
# - none: disables vector search (RAG)
RAG_PROVIDER=chromadb
RAG_COLLECTION_NAME=futebol_data
RAG_TOP_K=3

# Embeddings
EMBEDDER_MODEL=all-MiniLM-L6-v2
EMBEDDINGS_BATCH_SIZE=64

# Chroma (local dev)
CHROMA_PERSIST_DIR=.chromadb
CHROMA_DB_IMPL=duckdb+parquet

# Qdrant / Supabase pgvector are not implemented by default in the wrapper.
# If you implement them, add provider-specific env vars here.

# Operational
RAG_LOG_LEVEL=INFO
RAG_AUTO_PERSIST=true
